{
  "name": "tic-tac-toe",
  "version": "1.0.0",
  "description": "",
  "main": "src/maint.ts",
  "scripts": {
    "build": "tsc src/main.ts --outdir dist",
    "play": "npm run build && node dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.7.16"
  }
}
